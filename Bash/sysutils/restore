# Set keyboard repeat rate
xset r rate 200 50

# Swap caps and escape
setxkbmap -option caps:swapescape

# Map Hyper_L
xmodmap -e "keycode 134 = Hyper_L"

# US keyboard layout with deadkeys
setxkbmap us -variant intl

echo "Restore is DONE"

exit 0
